{
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && rm -rf server/dist && cp -r client/dist server/dist && npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
